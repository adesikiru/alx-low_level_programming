#include <stdio.h>

/**
 * main - Entry point
 * Descrption: Use main function to printsingle digit numbers of base 10
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');

	return (0);
}
