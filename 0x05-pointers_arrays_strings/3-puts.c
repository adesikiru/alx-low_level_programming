#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string, folllowed by new line,
 * @str: pointer to the string to print
 * Return: void
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
