#include "main.h"
#include <stdio.h>

/**
 * main - Entry Point
 * Description: a function that adds two
 * integers and returns the result.
 * Return: Always 0.
 */

int main(void)
{
	int n, a, b;

	n = add(a, b);
	return (n);
}
